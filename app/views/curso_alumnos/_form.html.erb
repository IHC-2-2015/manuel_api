<%= form_for(@curso_alumno) do |f| %>
  <% if @curso_alumno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curso_alumno.errors.count, "error") %> prohibited this curso_alumno from being saved:</h2>

      <ul>
      <% @curso_alumno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :curso_id %><br>
    <%= f.select :curso_id, Curso.all.collect {|c| [c.nombre, c.id]} %>
  </div>
  <div class="field">
    <%= f.label :alumno_id %><br>
    <%= f.select :alumno_id, Alumno.all.collect {|a| [a.nombre, a.id]} %>
  </div>
  <div class="field">
    <%= f.label :ayudante %><br>
    <%= f.check_box :ayudante %>
  </div>
  <div class="field">
    <%= f.label :estado %><br>
    <%= f.check_box :estado %>
  </div>
  <div class="field">
    <%= f.label :jefe_proyecto %><br>
    <%= f.check_box :jefe_proyecto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
